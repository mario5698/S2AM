import 'package:flutter/material.dart';

class Mock_Page extends StatelessWidget {
  const Mock_Page({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('mock page'),
    );
  }
}
